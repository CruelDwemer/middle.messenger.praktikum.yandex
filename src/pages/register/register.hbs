<div class="login-container">
    <form class="login-paper">
        <h2>Регистрация</h2>
        {{{ emailInput }}}
        {{{ loginInput }}}
        {{{ passwordInput }}}
        {{{ firstNameInput }}}
        {{{ secondNameInput }}}
        {{{ phoneInput }}}
        {{{ registrationButton }}}
        {{{ loginButton }}}
    </form>
</div>
